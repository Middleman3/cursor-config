# Rules

Cursor rule files (`.mdc`) loaded when this repo is used as the **.cursor** submodule. Not used by the skills CLI; for skills-only installs use `npx skills add <org>/cursor-config`. See root [README.md](../README.md).
